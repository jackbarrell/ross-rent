services:
  - type: web
    name: ross-rent
    runtime: node
    plan: free
    buildCommand: npm install --include=dev --prefix backend && npm run build --prefix backend && npm install --include=dev --prefix frontend && npm run build --prefix frontend
    startCommand: cd backend && node dist/index.js
    envVars:
      - key: NODE_VERSION
        value: "20.18.1"
      - key: PORT
        value: "10000"
      - key: USE_MOCK_DATA
        value: "true"
      - key: FRONTEND_ORIGIN
        value: "*"
